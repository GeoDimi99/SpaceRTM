services:
  
  memcached:
    image: memcached
    cpuset: "0"
    command:
      - --conn-limit=1024
      - --memory-limit=64
      - --threads=4
    ports:
      - "11211:11211"
  
  
  orchestrator:
    build: orchestrator
    cpuset: "0"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
